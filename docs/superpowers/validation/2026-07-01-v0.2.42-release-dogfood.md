# v0.2.42 Release and Dogfood Validation

## Summary

Released `v0.2.42` from `cd1a839 feat(core): add storage facade for project-local memories (#78)`.

This release ships Project-local Storage Defaults Slice 0: an internal storage facade proof that preserves existing default storage behavior while removing the engine's direct single-store assumptions.

## Release

- Tag: `v0.2.42`
- Source commit: `cd1a839`
- Release workflow: `28484161404`
- Release URL: https://github.com/ribbons-digital/memory-lane/releases/tag/v0.2.42
- Assets published: 8
  - `install.ps1`
  - `install.sh`
  - `memory-lane-darwin-arm64.tar.gz`
  - `memory-lane-darwin-x64.tar.gz`
  - `memory-lane-linux-arm64.tar.gz`
  - `memory-lane-linux-x64.tar.gz`
  - `memory-lane-windows-x64.exe.zip`
  - `SHA256SUMS`

## Pre-tag validation

Run locally on `main` at `cd1a839` before tagging:

```bash
pnpm build
pnpm test
git diff --check
```

Result: passed.

## GitHub release workflow validation

`gh run watch 28484161404 --exit-status` completed successfully.

Workflow steps passed:

- Checkout
- Install pnpm
- Install Node.js
- Install Bun
- Install dependencies
- Build packages
- Run tests
- Build binaries
- Smoke test current-platform binary
- Generate release notes
- Create release

## Installed-artifact dogfood

Installed latest release artifact and smoke-tested it:

```bash
memory-lane upgrade --yes
memory-lane --smoke-test
```

Result:

- Installer downloaded and verified `memory-lane-darwin-arm64.tar.gz`.
- `memory-lane` installed successfully to `/Users/shiang/.local/bin/memory-lane`.
- Previously installed `pi` harness configuration was reapplied.
- `memory-lane --smoke-test` returned `memory-lane ok`.

## Scope confirmation

This release does **not** flip default storage locations.
Project-scoped memories still write to the active resolved storage location unless project-local storage is explicitly initialized or home storage fallback applies.
Slice 1 remains a separate approval-gated follow-up.
