# v0.2.43 Release and Dogfood Validation

Released `v0.2.43` from `287bb1a docs: prepare v0.2.43 release status (#83)`.

This release ships the previously unreleased work from PR #80 and PR #82.
PR #80 adds project-local default writes for new project-scoped memories while preserving home-side global preferences and personal memories.
PR #82 adds harness-neutral continuity routing and continuity context hygiene across Claude/Codex hooks, repo-local Pi, generated Pi bridge routing, and MCP steering.

- Tag: `v0.2.43`
- Release workflow: `28560737645`
- Release URL: https://github.com/ribbons-digital/memory-lane/releases/tag/v0.2.43

## Pre-release validation

Before this release, PR #80 passed targeted core, CLI, MCP, and plugin tests, `pnpm build`, `git diff --check`, Opus 4.8 review, no-mistakes gates, GitHub CI, and CodeRabbit review.
Before this release, PR #82 passed Opus 4.8 focused reviews, `pnpm build`, `pnpm test`, `git diff --check`, no-mistakes gates, GitHub CI, and CodeRabbit review.
The release-prep PR #83 passed no-mistakes review, tests, docs, lint, push, PR, and CI gates.

## GitHub release workflow validation

Workflow `28560737645` completed successfully.
The release job completed:

- Build packages
- Run tests
- Build binaries
- Smoke test current-platform binary
- Generate release notes
- Create release

GitHub Actions emitted a non-blocking Node.js 20 deprecation annotation for action runtime compatibility.
It did not fail the workflow.

## Installed artifact dogfood

Installed latest release artifact and smoke-tested it:

```sh
memory-lane --smoke-test
memory-lane upgrade --yes
memory-lane --smoke-test
```

Results:

- `memory-lane --smoke-test` returned `memory-lane ok` before upgrade.
- `memory-lane upgrade --yes` downloaded `memory-lane-darwin-arm64.tar.gz`, verified checksum, installed successfully, and reconfigured the existing Pi integration.
- `memory-lane --smoke-test` returned `memory-lane ok` after upgrade.

## Continuity routing dogfood

Ran the installed `route` command against the motivating prompt:

```sh
memory-lane route --prompt "what's the next item we should work on and what's its scope?" --json
```

Result:

- The route was `continuity`.
- The detected family was `next-work`.
- Reasons included `next-token`, `work-item-token`, `next-question`, and `scope-next-work`.

## Generated Pi bridge dogfood

Loaded the installed generated Pi extension from `~/.pi/agent/extensions/memory-lane/index.ts` with `node --import tsx`.
The smoke verified that the generated bridge registered `/memory`, `memory_continuity`, and `before_agent_start`.
It then invoked `before_agent_start` with the motivating prompt in the Memory Lane project cwd.

Result:

- The generated bridge returned hidden `memory-lane` continuity context.
- The message content included `Memory Lane continuity context`.
- The message details reported `surface: "continuity"`.

## Outcome

`v0.2.43` release and installed-artifact dogfood passed.
The release ships project-local default writes and continuity routing/context hygiene without adding the deferred LLM classifier behavior.
The next recommended implementation slice remains project-local storage Slice 2 migration/compatibility diagnostics, pending a fresh design and approval gate.
