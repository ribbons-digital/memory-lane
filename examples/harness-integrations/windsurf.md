# Memory Lane Integration for Windsurf

## Setup

1. Build and link the CLI: see `claude-code.md`.
2. Add to your Windsurf global rules or project rules:

```
# Memory
Persist important facts with: `memory-lane save "X" --status approved`
Recall with: `memory-lane recall "query"`
List all: `memory-lane list`
```
